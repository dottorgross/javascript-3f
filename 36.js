// le funzioni oltre a fare delle operazioni
// possono anche restituire un risultato

function somma(numero1, numero2) {
    let somma = numero1 + numero2
    return somma
}

let espressione = 5 + somma(2, 3)

console.log(espressione)